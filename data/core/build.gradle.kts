plugins {
    id("lifemash.android.library")
    id("lifemash.android.hilt")
}

android {
    namespace = "org.bmsk.lifemash.data.core"
}

dependencies {
    // Core data dependencies can be added here later
}